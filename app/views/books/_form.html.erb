

<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li>
          <div class="alert alert-info">
          <strong>Info!</strong> Indicates a neutral informative change or action.
          </div><%= message %>
        </li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="alert alert-warning" role="alert">
    <%= notice %>  
  </div>
<table class='field'>
 <tr>
  <td><%= form.label :bname %></td>
  <td><%= form.text_field :bname %></td>
 </tr>
<tr>
  <td><%= form.label :bwriter %></td>
  <td><%= form.text_area :bwriter %></td>
</tr>
<tr>
  <td><%= form.label :bprise %></td>
  <td><%= form.number_field :bprise %></td>
</tr>
<tr>
  <td><%= form.label :description %></td>
  <td><%= form.text_area :description %></td>
</tr>
<tr>
  <td><%= form.label :No_of_pages %></td>
  <td><%= form.number_field :No_of_pages %></td>
</tr>
</table>

  <div class="actions">
  &nbsp;&nbsp;&nbsp;&nbsp;<%= form.submit %>
  </div>
<% end %>
